<%= form_for(@student_skill) do |f| %>
  <% if @student_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_skill.errors.count, "error") %> prohibited this student_skill from being saved:</h2>

      <ul>
      <% @student_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.text_field :skill_id %>
  </div>
  <div class="field">
    <%= f.label :experience %><br>
    <%= f.text_area :experience %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
